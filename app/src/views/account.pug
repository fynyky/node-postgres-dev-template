extends base.pug
block main
  .container
    
    h1.title Account information

    p.block= JSON.stringify(user)

    form(action="logout" method="POST")
      .field
        .control
          button.button.is-light(type="submit") Logout
